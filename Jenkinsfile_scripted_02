node {
    def mvnHome
    stage('Preparation') { // for display purposes
        // Get some code from a GitHub repository
       echo 'stage - Preparation'
       sh "sudo orca --service agent_matching --no-version --soft-validations --lpsegment alpha -d Virginia --servers avor-jet114 --batch 1 --fact agent_matching_d6r --state_filename jenkins-puppet-messageRouting-app-subtask-26"
    }
    stage('Build') {
        // Run the maven build
        echo 'Stage - Build'
    }
    stage('Results') {
       echo 'Stage : Results '
    }
}
